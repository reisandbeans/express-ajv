import { SchemaCollection, ValidatorMiddleware, ValidatorOptions } from './types';
export declare function createValidator(schemas: SchemaCollection, options?: ValidatorOptions): ValidatorMiddleware;
